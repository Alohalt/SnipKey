# 需求变更记录

用于持续记录产品需求、交互约束和交付边界的变化，便于后续开发、验收和回溯。

## 记录规则

- 每条记录应包含日期、变更内容、原因和影响范围。
- 若变更影响交互或数据结构，应同步更新 README 与相关设计文档。
- 若变更只属于实现细节优化，不进入本表；只有会影响产品行为或交付范围的调整才记录。

## 变更明细

| 日期 | 类型 | 需求变更 | 原因/来源 | 影响范围 |
| --- | --- | --- | --- | --- |
| 2026-04-15 | 基线 | 项目定义为 macOS 菜单栏文本扩展工具，核心交互为在任意应用中输入 `#trigger` 后展开预设文本。 | 初始设计方案 | 键盘监听、补全面板、文本替换、菜单栏形态 |
| 2026-04-15 | 基线 | 设置界面需要支持分组管理、Key 列表、详情编辑，以及首次运行权限与使用引导。 | 初始设计方案 | 设置窗口、数据模型、入门体验 |
| 2026-04-17 | 范围扩展 | 新增剪贴板历史记录与“重复复制内容建议创建 Key”能力，作为 Key 管理入口的补充。 | 使用效率需求补充 | 剪贴板监听、历史存储、设置入口、提示流程 |
| 2026-04-17 | 交互调整 | 设置界面改为更接近原生 macOS 的三栏浏览结构，并强化搜索、自动保存与未保存替换内容保护。 | 当前分支 UI/交互打磨 | `SettingsView`、编辑流程、一致性校验 |
| 2026-04-17 | 体验调整 | 首次启动时自动打开设置页并展示聚焦式引导，且允许用户后续手动重新打开。 | 降低首次使用门槛 | onboarding、设置页入口、引导状态持久化 |
| 2026-04-17 | 体验调整 | 从剪贴板内容创建 Key 时，默认使用本地规则生成简短 trigger：默认 4 位、英文短语取单词首字母、纯编码优先保留后缀并按冲突逐位扩展。 | 降低创建成本，同时让默认命名更接近高频实际使用方式 | `SnippetTriggerSuggester`、剪贴板建 Key 流程、README |
| 2026-04-17 | 排序策略 | 候选 Key 的匹配结果按接受次数优先排序，精确命中仍保持最高优先级。 | 提升高频 Key 命中效率 | `SnippetEngine` 排序逻辑、候选展示 |
| 2026-04-17 | 交付约束 | 开发运行方式以签名后的固定路径应用包为主，不再把 `swift run` 视为默认调试流程。 | macOS 权限与稳定性约束 | README、开发流程、签名文档 |
| 2026-04-20 | 交互约束 | Key 的 trigger 收紧为仅支持字母、数字和下划线，并执行大小写不敏感的唯一性校验；已有非法或重复 trigger 在加载与导入时自动归一化。 | 中文 trigger 在全局输入监听链路中稳定性不足，改为统一 ASCII trigger 约束以确保行为可预期。 | `SnippetStore`、`SettingsView`、`KeyboardMonitor`、导入/加载流程、README、设计文档 |

## 后续追加模板

可按下面格式继续追加：

```md
| 2026-04-18 | 范围扩展 | 示例变更内容 | 需求评审 / 用户反馈 / 技术约束 | 受影响模块 |
```